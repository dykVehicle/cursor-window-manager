# Cursor Rules 模板

## 1. 项目目录确认规则

在执行任何任务之前，必须：
- 明确确认当前操作的项目目录路径
- 如果用户未指定项目，主动询问或根据上下文推断目标项目
- 在开始修改文件前，先列出项目结构确认无误
- 避免跨项目误操作

## 2. 日志读取规则

在修复问题或调试时，必须：
- 优先读取项目的关键日志文件（如 `logs/`、`*.log`、控制台输出）
- 检查错误堆栈和警告信息
- 根据日志定位问题根源，而非盲目猜测
- 常见日志路径参考：
  - Node.js: `npm-debug.log`、`yarn-error.log`
  - Python: `logs/*.log`、控制台输出
  - 前端构建: `dist/`、终端构建输出

## 3. Token 节省规则

为节省 tokens，遵循以下原则：
- 只读取必要的文件和代码片段，避免全量读取大文件
- 使用 grep/搜索精准定位代码，而非逐文件浏览
- 回复简洁扼要，避免重复已知信息
- 代码修改使用精确的 search_replace，而非重写整个文件
- 合并相关操作，减少来回确认
- 不展示无关的上下文代码

## 4. 自动编译规则

任务完成后，如果项目需要编译/构建：
- 自动执行构建命令生成产物
- 常见构建命令：
  - Node.js/前端: `npm run build` / `yarn build` / `pnpm build`
  - Python: `pyinstaller`、`pip install -e .`
  - TypeScript: `tsc` / `npm run build`
- 构建完成后，明确告知编译产物路径
- 如果构建失败，读取错误日志并修复

## 5. 自动验证规则

每次代码修改后，进行适当验证：
- 可自动验证的操作（优先执行）：
  - 运行 linter 检查语法错误
  - 执行类型检查（TypeScript: `tsc --noEmit`）
  - 运行单元测试（如果存在且快速）
- 需要平衡 tokens 消耗：
  - 简单修改：仅检查 linter 错误
  - 复杂修改：运行相关测试用例
  - 大规模重构：完整测试 + 构建验证
- 验证失败时，自动读取错误并修复

## 6. 输出总结规则

每次任务完成时，在回复末尾提供执行摘要：
```
---
📊 执行摘要
- 模型: [当前使用的模型名称]
- 本次 Token: 输入 ~XXX | 输出 ~XXX
- 本次费用: ~$XXX / ~¥XXX
- 累计 Token: 输入 ~XXX | 输出 ~XXX
- 累计费用: ~$XXX / ~¥XXX
- 执行耗时: ~XX 秒
- 任务状态: ✅ 完成 / ⚠️ 部分完成 / ❌ 失败
```
注意：Token 和耗时为估算值，供参考；token的消耗需要根据实际的token使用情况进行计算，并根据不同模型的token价格进行计算。累计统计应贯穿整个对话会话。

## 7. 代码提交提示规则

当任务涉及代码修改时，在执行摘要后提供 Git 推送命令建议：
- 仅提供命令，**不自动执行推送**
- 包含 add、commit、push 完整流程
- commit message 应简洁描述未提交的修改内容

示例格式：
```
📤 推送命令（请手动执行）
git add .
git commit -m "描述本次修改"
git push origin <branch>
```

## 项目类型识别

根据以下文件自动识别项目类型：
- `package.json` → Node.js/前端项目 → 产物目录: `dist/`、`build/`
- `requirements.txt` / `setup.py` → Python 项目 → 产物目录: `dist/`
- `Cargo.toml` → Rust 项目 → 产物目录: `target/release/`
- `go.mod` → Go 项目 → 产物目录: 项目根目录
- `pom.xml` / `build.gradle` → Java 项目 → 产物目录: `target/`、`build/`

## 语言偏好

- 始终尽量使用中文回复
